import { offersContent } from './offers'

export const commonContent = {
  appName: offersContent.title,
  appTagline: offersContent.subtitle,
  retry: 'Tentar novamente',
  back: 'Voltar',
} as const
